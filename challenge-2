function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);



  for (var y = 10; y <= 388; y = y + 10) {

    for (var x = 12; x <= 388; x = x + 10) {
      ellipse(x, y, 5, 5);
    }
  }

}
